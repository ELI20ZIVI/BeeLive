pub async fn is_resource_locked (event_id: u32) -> bool {
    // Componente non ancora implementato - Per sprint futuri
    false
}

pub async fn is_resource_locked_by_user (event_id: u32, user_id: &str) -> bool {
    // Componente non ancora implementato - Per sprint futuri
    true
}