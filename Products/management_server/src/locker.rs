use crate::dao::objects::User;

pub async fn is_resource_locked (_event_id: u32) -> bool {
    // Componente non ancora implementato - Per sprint futuri
    false
}

pub async fn is_resource_locked_by_user (_event_id: u32, _user: &User) -> bool {
    // Componente non ancora implementato - Per sprint futuri
    true
}
